name: jev-layer
version: "1.1.0"
description: "Jev decision layer: prompt enhancement on every user turn (pre_llm_call), safety gate on terminal commands (pre_tool_call), always-on Jev rules in the system prompt. Fail-open: if Jev is unreachable everything runs as before."
